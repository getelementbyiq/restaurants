import React from "react";
import PropTypes from "prop-types";
import { Box } from "@mui/material";

const MainProducts = (props) => {
  return <Box>MainProducts</Box>;
};

MainProducts.propTypes = {};

export default MainProducts;
